CREATE TABLE menu (
    id INT AUTO_INCREMENT,
    cafeteria VARCHAR(20) NOT NULL,
    name VARCHAR(20) NOT NULL,
    price VARCHAR(20),
    image VARCHAR(255),
    content VARCHAR(255),
    PRIMARY KEY (id)
);